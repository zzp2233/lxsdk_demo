#ifndef _ROTARY_H
#define _ROTARY_H

/**
 * @brief 转盘消息处理
 * @param[in] rotary : 列表指针
 * @param[in] rotary : 消息事件
 * @return 返回处理结果
 **/
bool compo_rotary_message(compo_rotary_t *rotary, size_msg_t msg);

#endif
